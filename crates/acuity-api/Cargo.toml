[package]
name = "acuity-api"
version = "0.1.0"
edition = "2024"
rust-version = "1.98"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
acuity-schema = { path = "../acuity-schema" }
