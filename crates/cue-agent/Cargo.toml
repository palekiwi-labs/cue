[package]
name = "cue-agent"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
cuelib = { path = "../cuelib" }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
assert_cmd = "2.2.1"
predicates = "3.1.4"
tempfile = "3.27.0"
