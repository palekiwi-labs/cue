# Cue CLI Specification

## Purpose

`cue` is the CLI tool for creating and managing cue protocol artifacts. It is
the primary interface for both humans and agents interacting with the `.cue/`
memory system.

## Crate Dependencies

- `cuelib`: artifact/filesystem domain.

## Reference

See the `cue` agent skill for the full protocol description, artifact types,
and command reference.
